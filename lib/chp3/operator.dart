void main(){
  //operator that is used to perform operation on operands such as +,-,*,/,%
  //operands are the values on which operation is performed such as a + b,a-b , a*b
  int a=10;
  int b=10;
  print(a+b);
  print(a-b);
  print(a*b);
  print(a/b);
  print(a~/b); //floor Divison and ~it's mean tild operator
  print(a%b);//modulue
 // What is an expression ?
  /*
    An expression is a special kind of statement that evaluates to a value. Every expression is composed of −

    Operands − Represents the data

    Operator − Defines how the operands will be processed to produce a value.*/







}